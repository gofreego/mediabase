apiVersion: apps/v1
kind: Deployment
metadata:
  name: mediabase
  labels:
    app: mediabase
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mediabase
  template:
    metadata:
      labels:
        app: mediabase
    spec:
      containers:
        - name: mediabase
          image: ghcr.io/gofreego/mediabase:pilot-1be27cd
          imagePullPolicy: Always
          command:
            - /app/application
          args:
            - -env=prod
            - -path=/etc/mediabase/config
          ports:
            - name: http
              containerPort: 80
            - name: grpc
              containerPort: 8081
          volumeMounts:
            - name: config
              mountPath: /etc/mediabase/config
              readOnly: true
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
      volumes:
        - name: config
          secret:
            # Created out-of-band from the private configs-prod repo, not by
            # anything in this repo's own manifests - see infra's
            # mediabase-config-secret role.
            secretName: mediabase-config
