apiVersion: v1
kind: Service
metadata:
  name: mediabase
spec:
  selector:
    app: mediabase
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: grpc
      port: 8081
      targetPort: grpc
